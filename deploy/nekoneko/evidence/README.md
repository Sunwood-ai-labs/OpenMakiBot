# Evidence images

`official-*` and `official-run.json` are from the official v0.1.61 checkout plus article settings, validated September 7-8, 2026. Each bot used its own GUI and saved three PNGs. Matching screenshots can have identical bytes when both browsers render the same HTML in the same state.

`computer-panel.png` and the unprefixed `kinako-*.png` are legacy development evidence. See `../LEGACY-VALIDATION.md` for that older configuration.

The public HTML example is `../example/index.html`. It is the final HTML generated in the official-source validation run. Raw conversations and connection credentials are not included.
